# XRP Watchdog Configuration
# Copy this file to config.env and customize for your environment

# Rippled Configuration
# The Docker container name or 'local' for non-Docker rippled
RIPPLED_CONTAINER=rippledvalidator

# ClickHouse Ports
# HTTP API port (default: 8123)
CLICKHOUSE_HTTP_PORT=8123
# Native protocol port (default: 9000)
CLICKHOUSE_NATIVE_PORT=9000

# ClickHouse Database
CLICKHOUSE_HOST=localhost
CLICKHOUSE_DB=xrp_watchdog

# Collection Settings
# How many ledgers to collect per run (default: 13 for 15-min schedule)
COLLECTION_BATCH_SIZE=13

# Auto-collection schedule (cron format)
# Default: "*/15 * * * *" (every 15 minutes)
# Collects ~1,248 ledgers/day (~5.8% coverage)
CRON_SCHEDULE="*/15 * * * *"
